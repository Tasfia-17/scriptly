name: scriptly_client
description: Client package for Scriptly Serverpod backend
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  serverpod_flutter: ^2.0.0
  serverpod_auth_client: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
